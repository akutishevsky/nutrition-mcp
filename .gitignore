# dependencies (bun install)
node_modules

# output
out
dist
*.tgz

# code coverage
coverage
*.lcov

# logs
logs
_.log
report.[0-9]_.[0-9]_.[0-9]_.[0-9]_.json

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# supabase (local CLI state only — track config.toml and migrations)
supabase/.temp/
supabase/.branches/

# caches
.eslintcache
.cache
*.tsbuildinfo

# IntelliJ based IDEs
.idea

# Finder (MacOS) folder config
.DS_Store

# Static site pages generated by scripts/gen-*.ts (bun run gen:all) — rebuilt
# on every Docker build and in CI before tests run. Source of truth is
# src/copy/*.ts and src/routes.ts, not these files.
/public/index.html
/public/tools.html
/public/privacy.html
/public/terms.html
/public/login.html
/public/sitemap.xml
/public/alternatives/
/public/de/
/public/es/
/public/fr/
/public/nl/
/public/pl/
/public/it/
/public/uk/
/public/ja/

# The landing page's widget card assets, generated by
# scripts/gen-widget-card.ts from the shared partials under
# public/widgets/src/. Same deal as the pages above: `bun run gen:all` builds
# them in CI and in the Docker image, and src/widget-card.test.ts fails if what
# is on disk is not a fresh build of the partials.
/public/widget-card.css
/public/widget-card.js
/public/widget-card.*.js
